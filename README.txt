# Libraries dependensies
pip install flask-security-too sqlalchemy bcrypt
pip install flask_babelex
pip install Flask-Mail
pip install python-dotenv
pip install psycopg2	//if the DBMS used is postgreSQL
